<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>h3sync 控制台</title>
  <link rel="stylesheet" href="/static/admin.css" />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" />
  <script src="/static/admin.js"></script>
</head>
<body>
  <div class="app-container">
    {{template "sidebar" .}}

    <div class="mobile-header">
      <div style="font-weight: 800; font-size: 1.2rem;">h3sync</div>
      <button class="menu-toggle">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
      </button>
    </div>

    <main class="main-content">
      <div class="card">
        <h1 class="card-title">欢迎使用 h3sync 控制台</h1>
        <div style="color: var(--text-muted);">
          <p>h3sync 是专为瑞普生物设计的数据采集与同步管理平台。</p>
          <br>
          <p>目前系统已支持：</p>
          <ul style="margin-left: 20px; margin-top: 10px;">
            <li>氚云 (H3) 业务数据的增量同步</li>
            <li>MSSQL 备份 SQL 文件的全量/增量自动化导入</li>
            <li>基于 API Key 的安全数据查询 API (AI 可读)</li>
          </ul>
        </div>
      </div>

      <div class="grid-cols-2">
        <div class="card">
          <h3 class="card-title">数据同步状态概览</h3>
          <p style="font-size: 0.875rem; color: var(--text-muted);">查看最近 24 小时的同步任务执行结果。</p>
          <div style="margin-top: 16px;">
             <a href="/admin/forms" class="btn btn-primary">立即管理</a>
          </div>
        </div>
        <div class="card">
          <h3 class="card-title">安全管理</h3>
          <p style="font-size: 0.875rem; color: var(--text-muted);">为 AI 平台生成新的查询凭证。</p>
          <div style="margin-top: 16px;">
             <a href="/admin/apikeys" class="btn">生成 API Key</a>
          </div>
        </div>
      </div>
    </main>
  </div>
</body>
</html>
